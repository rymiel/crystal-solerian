#! /usr/bin/env crystal
#
# To build a standalone command line client, require the
# driver you wish to use and use `Micrate::Cli`.
#

require "micrate"
require "../src/config"
require "sqlite3"

Micrate::DB.connection_url = SolHTTP.config.db_uri
Micrate::Cli.run